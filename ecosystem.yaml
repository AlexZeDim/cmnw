apps:
  - script: ./bin/getTokens.js
    name: 'getToken'
    instances: 1
    watch: true
    autorestart: true
    max_memory_restart: '100M'
    cron_restart: '*/1 * * * *'
    log_date_format: 'DD/MM HH:mm:ss.SSS'